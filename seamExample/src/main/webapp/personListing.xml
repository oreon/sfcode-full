<!DOCTYPE composition PUBLIC "-//W3C//DTD XHTML 1.0 Transitional//EN" "http://www.w3.org/TR/xhtml1/DTD/xhtml1-transitional.dtd">
<ui:composition xmlns="http://www.w3.org/1999/xhtml"
                xmlns:ui="http://java.sun.com/jsf/facelets"
                xmlns:h="http://java.sun.com/jsf/html"
                xmlns:f="http://java.sun.com/jsf/core"
                xmlns:s="http://jboss.com/products/seam/taglib"
                xmlns:a="http://richfaces.org/a4j"
                template="template.xhtml">

<!-- content -->
<ui:define name="content">

  <!-- put global messages at the top! -->
  <div class="section">
    <span class="errors">
      <h:messages globalOnly="true"/> 
    </span>

    <h1>Search persons</h1>

    <h:form id="searchCriteria">
      <fieldset> 
      
        &#160;
	<!-- the reRender attribute needs to have the same name as the id for the outputPanel below -->
        <a:commandButton id="findpersons" value="Find persons" action="#{personAction.findpersons}" reRender="searchResults"/>
       &#160;
       <!-- RichFaces tag that displays an image while waiting on an asynchronous request to return -->
       <a:status>
         <f:facet name="start">
           <h:graphicImage value="/img/spinner.gif"/>
         </f:facet>
       </a:status>

       <br/>

       <h:outputLabel for="pageSize">Maximum results:</h:outputLabel>&#160;
       <h:selectOneMenu value="#{personAction.pageSize}" id="pageSize">
          <f:selectItem itemLabel="5" itemValue="5"/>
          <f:selectItem itemLabel="10" itemValue="10"/>
          <f:selectItem itemLabel="20" itemValue="20"/>
       </h:selectOneMenu>
      </fieldset>
    </h:form>
  </div>

  <a:outputPanel id="searchResults">
    <div class="section">
      <!-- the DataList, persons is not a List, but rather a javax.faces.model.DataModel -->
      <h:outputText value="No persons could be found." rendered="#{persons != null and persons.rowCount==0}" />
      <h:dataTable id="persons" value="#{persons}" var="person" rendered="#{persons.rowCount > 0}">
	<h:column>
	  <f:facet name="header">First Name</f:facet>
	  #{person.firstName}
	</h:column>

	<h:column>
	  <f:facet name="header">Last Name</f:facet>
	  #{person.lastName}
	</h:column>

	<h:column>
	  <f:facet name="header">Created By</f:facet>
	  #{person.createdByUser.name}
	</h:column>

	<h:column>
	  <f:facet name="header">Action</f:facet>
	  <s:link id="viewperson"
		  value="View person"
		  action="#{personAction.selectperson(person)}" />
	</h:column>
      </h:dataTable>
      <s:span rendered="#{personAction.previousPageAvailable}">
	<s:link value="Previous results" action="#{personAction.previousPage}"/>&#160;
      </s:span>
      <s:link value="More results"
	      action="#{personAction.nextPage}"
	      rendered="#{personAction.nextPageAvailable}"/>
      <br />
      <h:outputText value="Showing records: #{personAction.firstRecord} to #{personAction.lastRecord} of #{personAction.totalRecords}" rendered="#{personAction.totalRecords > 0}"/>
    </div>
  </a:outputPanel>

</ui:define>
</ui:composition>
