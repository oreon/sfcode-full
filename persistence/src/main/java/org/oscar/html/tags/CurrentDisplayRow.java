package org.oscar.html.tags;

/**
 * Tabletag needs to tell column tag which kind of row it currently is at 
 * @author jsingh
 *
 */
public enum CurrentDisplayRow {
	MODE_HEADER,
	MODE_FILTER,
	MODE_NORMAL
}
